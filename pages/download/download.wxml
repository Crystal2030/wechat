<!--pages/mine/resources/resources.wxml-->
<view>
  <block wx:for="{{resources}}" wx:for-item="resource" wx:key="unique" class="resource-container">
    <text data-categoryId="{{resource.category_id}}" catchtap="goDownloadDetail">{{resource.category_name}}</text>
  </block>
</view>
